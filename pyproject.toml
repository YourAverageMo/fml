[project]
name = "fml"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-genai>=1.18.0",
]

[dependency-groups]
dev = [
    "pytest>=8.3.5",
]
